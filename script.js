'use strict'
const startButton = document.getElementById('menu')
startButton.addEventListener('click', (x) => {
    startButton.innerHTML = 'GO!';
    //document.getElementsByClassName('stone')[0].style.left = x + 'px';
});
